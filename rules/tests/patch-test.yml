openapi: 3.0.1
paths:
    /ko-patch:
      patch:
        requestBody:
          content:
            application/json:
              schema:
                type: string
    /working-patch:
      patch:
        requestBody:
          content:
            application/json-patch+json:
              schema:
                type: string
    /ko-get-with-body:
      get:
        requestBody:
          content:
            application/json: {}
    /ok-get-with-body:
      get: {}
            
